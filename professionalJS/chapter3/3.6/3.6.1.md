# if语句

```
if (condition) statement1 else statement2
1. condition可以是任意表达式，这个表达式求值不一定是布尔值，会自动调用Boolean()转换
```

推荐的写法

```
if (i > 25) {
    alert('')
} else if (i < 0) {
    alert(')
} else {
    alert(')
}
```